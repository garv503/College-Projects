"""Business logic.

Routes in `routes/` handle HTTP concerns - reading the request, checking
permissions, shaping the response. Everything that is a *decision* about
the data lives here, so it can be tested without a web request and reused
by more than one route.
"""
