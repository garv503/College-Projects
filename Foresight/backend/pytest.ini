[pytest]
# `tests` is on the path so the test modules can `from conftest import ...`,
# and the backend root so they can import `app`, `db`, `services.*`.
pythonpath = . tests
testpaths = tests
python_files = test_*.py
addopts = -q --strict-markers --tb=short
filterwarnings =
    ignore::DeprecationWarning
